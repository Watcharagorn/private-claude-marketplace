---
description: Guided loom setup — verify the plugin + hook, opt into tracking, learn the harvest/learn modes, optionally schedule the daily automation, and finish with a verification dry-run; resumable, re-runs skip what's done
argument-hint:
allowed-tools: [Bash, Read, Write, Edit, Skill, Task, AskUserQuestion]
---

# loom — onboard

Follow the `onboard` skill end to end: a resumable, step-by-step walkthrough that sets up everything
loom needs (tracking, optional daily automation) and teaches the harvest/learn modes along the way.
Each step checks current state first, so re-running skips what's already configured.

$ARGUMENTS
