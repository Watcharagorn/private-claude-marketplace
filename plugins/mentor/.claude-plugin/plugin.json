{
  "name": "mentor",
  "description": "A lean planning harness for Claude Code: a marker-driven edit gate keeps the repo read-only until a Mermaid-first Markdown plan is approved, then implementation runs subagents-first (SDD); a context gate guards the session window.",
  "version": "2.6.0",
  "author": {
    "name": "NTBX"
  }
}
