{
  "name": "private-marketplace",
  "owner": {
    "name": "my-private"
  },
  "plugins": [
    {
      "name": "mentor",
      "description": "A lean planning harness for Claude Code: a marker-driven edit gate keeps the repo read-only until a Mermaid-first Markdown plan — constitution-checked and refined by a staged review — is approved; implementation then runs subagents-first (SDD). Oversized plans split into isolated siblings, per-plan state tracks what is built, and an ask-first context gate steers long sessions to a clean hand-off.",
      "source": "./plugins/mentor",
      "category": "development"
    },
    {
      "name": "sdlc-mini",
      "description": "Turbo SDLC helpers: a release-notes generator and a tidy commit helper. Manifest and env-config setup have moved to the devflow plugin; init-turbo-manifest and init-turbo-env-config remain as deprecated redirects.",
      "source": "./plugins/sdlc-mini",
      "category": "development"
    },
    {
      "name": "loom",
      "description": "Session harvesting and plugin lifecycle tools: harvest a session (or every un-harvested session of a project) into reusable Claude Code artifacts, audit an existing plugin from one session to fix how it misbehaved, learn from every session that used a plugin — or one named session — with both audit + enhance lenses, opt in to usage tracking that makes learning instant, and publish releases with synced manifests.",
      "source": "./plugins/loom",
      "category": "development"
    }
  ]
}
